name: ghostfacenet

weights_path: "deepface"

input_size: 112

embedding_dim: 512

normalization_mean:
  - 0.5
  - 0.5
  - 0.5

normalization_std:
  - 0.5
  - 0.5
  - 0.5

distance_metric: cosine

batch_size_override: 16
